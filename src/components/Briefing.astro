---
import type { BriefingData } from "../lib/sample-data";
interface Props {
  data: BriefingData;
  heading: string;
}
const { data, heading } = Astro.props;
---
<section class="section">
  <h2>{heading}</h2>
  <hr class="rule">
  <p class="briefing">{data.text}</p>
</section>
